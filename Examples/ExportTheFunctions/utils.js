console.log('hello')

const name = 'Vishnu'

const add = (a, b) => {
    return a + b
}

/** If we need to export the functions or varible to another filesystem we can use this funtion to do so */
module.exports = add